#!/bin/sh
if [ -z "$2" ]; then
	(echo "$1"; sleep 5) | \
	dzen2 -x 10 -y 10 -w 200 -h 50 -bg '#202020' -fn "Terminus:size=9" -fg '#999999'
else
	echo "^ca(1, pkill -n dzen2; $2 &)$1^ca()" | \
	dzen2 -x 10 -y 10 -w 200 -h 50 -bg '#202020' -fn "Terminus:size=9" -fg '#999999' -p
fi
	
